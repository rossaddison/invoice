/* 
 * ⚠️  Angular Production Environment
 * 📝 This configures Angular for production integration with Yii3
 */

export const environment = {
  production: true,
  apiUrl: '', // Use relative URLs in production
  yiiIntegration: true,
  debugMode: false
};